//! Cross-crate `Engine` scenarios that do not belong in a single stdlib package.
//!
//! Cases live in `tests/js` (and `tests/js/quickjs_wasi`) and are driven by
//! `tests/e2e.rs`, one file per scenario like `den-stdlib-wasm/tests/webassembly`.
